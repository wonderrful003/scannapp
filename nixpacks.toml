[phases.setup]
nixPkgs = ["tesseract", "python312"]

[phases.build]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn scannapp.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120"