[build]
builder = "NIXPACKS"
buildCommand = "bash build.sh"

[deploy]
startCommand = "gunicorn scannapp.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
healthcheckPath = "/api/health/"
restartPolicyType = "ON_FAILURE"

